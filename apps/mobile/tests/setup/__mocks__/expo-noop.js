// No-op stub for Expo's winter runtime modules that cannot run in a Jest/Node
// environment. These modules set up import.meta registry and hot-module reload
// machinery that is only meaningful in the Expo bundler runtime, not in tests.
module.exports = {};
